# animated-house_price-system
Predicts house prices using Traditional ML (Random Forest) and Deep Learning (DNN). Features include location, size, and amenities. The workflow uses Scikit-Learn for ML and TensorFlow/Keras for DL. Results show the DL model (R² ~0.90) significantly outperforms the ML model (R² ~0.77). Includes advanced preprocessing and visualizations.
